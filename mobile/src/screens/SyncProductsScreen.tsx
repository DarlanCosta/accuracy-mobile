import { VStack, Text } from 'native-base';

export const SyncProductsScreen = () => {
  return (
    <VStack flex={1} backgroundColor='bgColor' justifyContent='center' alignItems='center'>
      <Text color='gray.500'>Sincronizar</Text>
    </VStack>
  );
};
